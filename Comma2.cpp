#include<iostream>
using namespace std;

int main()
{
    int a;
    a=1,2,3;// when () brackets are not used then the order of associativity is from left to right and if the brackets are used then order of asociativity is from right to left
    cout<<a<<endl;
    return 0;
}         
             
             