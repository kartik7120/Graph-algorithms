#include<iostream>
using namespace std;

int main()
{
    int a=1;
    cout<<"Enter the value of a"<<endl;
    cin>>a; //The cin operator will always the variable type you use it with , in this case a is an integer and cin can only take input of an integer ONLY
    cout<<"Value of a ="<<" "<<a<<endl;
    return 0;
}